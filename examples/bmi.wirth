# 9. BMI calculation
height = 176
weight = 64
height_m = height / 100
bmi = weight / (height_m * height_m)
print "BMI is", bmi
if bmi < 18.5 print "low weight" endif
if bmi >= 18.5 and bmi < 25.0 print "normal weight" endif
if bmi >= 25.0 and bmi < 30.0 print "obesity level 1" endif
if bmi >= 30.0 and bmi < 35.0 print "obesity level 2" endif
if bmi >= 35.0 and bmi < 40.0 print "obesity level 3" endif
if bmi >= 40.0 print "obesity level 4" endif
